; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env]
monitor_speed = 115200
; defaulting to board version v1.0.0 : change that here if needed !
build_flags =
    -D HW_VERSION_MAJOR=1
    -D HW_VERSION_MINOR=0
    -D HW_VERSION_PATCH=0
; by default : upload and monitor

;targets = upload, monitor

; ================ UNIFIED CAN MOTOR  CODE (MIT) =================

[env:MIT_CAN_teensy41]
platform = teensy
board = teensy41
framework = arduino
build_src_filter =
    -<*>
    +<CAN_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_TEENSY41
    -D MIT_MODE
lib_deps =
    https://github.com/tonton81/FlexCAN_T4.git

[env:MIT_CAN_uno_r4]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<CAN_unified.cpp>
build_flags =
    ${env.build_flags}
    -D MIT_MODE
    -D PLATFORM_RENESAS_RA

; ================ UNIFIED CAN MOTOR  CODE (Servo) =================

[env:Servo_CAN_teensy41]
platform = teensy
board = teensy41
framework = arduino
build_src_filter =
    -<*>
    +<CAN_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_TEENSY41
    -D SERVO_MODE
lib_deps =
    https://github.com/tonton81/FlexCAN_T4.git

[env:Servo_CAN_uno_r4]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<CAN_unified.cpp>
build_flags =
    ${env.build_flags}
    -D SERVO_MODE
    -D PLATFORM_RENESAS_RA
; ================UNIFIED  LOAD CELL CODE =================

[env:LC_teensy41]
platform = teensy
board = teensy41
framework = arduino
build_src_filter =
    -<*>
    +<LoadCell_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_TEENSY41

[env:LC_uno_r4]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<LoadCell_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_RENESAS_RA

[env:LC_uno_r3]
platform = atmelavr
board = uno
framework = arduino
build_src_filter =
    -<*>
    +<LoadCell_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_ATMEL_AVR

; ============== UNIFIED ENCODER CODE  ======================

[env:encoders_teensy41]
platform = teensy
board = teensy41
framework = arduino
build_src_filter =
    -<*>
    +<Encoders_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_TEENSY41

[env:encoders_uno_r4]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<Encoders_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_RENESAS_RA

[env:encoders_uno_r3]
platform = atmelavr
board = uno
framework = arduino
build_src_filter =
    -<*>
    +<Encoders_unified.cpp>
build_flags =
    ${env.build_flags}
    -D PLATFORM_ATMEL_AVR



;------ justr a test for MIT mode

[env:MIT_DEBUG_R4]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<cubemars_com_arduino_mit.cpp>
build_flags =
    -D PLATFORM_RENESAS_RA
targets = upload, monitor

[env:SERVO_DEBUG_R4]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<main.cpp>
build_flags =
    -D PLATFORM_RENESAS_RA
targets = upload, monitor

; ============= SPI TESTS ===============

[env:spi_nano_master]
;build environment forload cell reading script. Works only on the Arduino R4
platform = nordicnrf52
board = nano33ble
framework = arduino
build_src_filter =
    -<*>
    +<../test/spi_test/**>
    -<../test/spi_test/arduino_slave.cpp>
targets = upload, monitor

[env:spi_uno_slave]
;build environment forload cell reading script. Works only on the Arduino R4
platform = atmelavr
board = uno
framework=arduino
build_src_filter =
    -<*>
    +<../test/spi_test/**>
    -<../test/spi_test/arduino_master.cpp>
targets = upload, monitor

;========

[env:SoftSPI]
platform =renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<../test/SoftSPITestScript.cpp>
lib_deps =
    robtillaart/SWSPI@^0.1.3
targets = upload, monitor

[env:ENCODER-DEBUG]
platform =renesas-ra
board = uno_r4_wifi
framework = arduino
build_src_filter =
    -<*>
    +<../test/encoder-test.cpp>
targets = upload, monitor